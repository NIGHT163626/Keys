local ScreenGui = Instance.new("ScreenGui")
local Frame = Instance.new("Frame")
local TituloLabel = Instance.new("TextLabel")
local NotificacaoLabel = Instance.new("TextLabel")
local TextBox = Instance.new("TextBox")
local BotaoVerificar = Instance.new("TextButton")
local StatusLabel = Instance.new("TextLabel")
local BotaoPegarChave = Instance.new("TextButton")

-- Adicionar GUI ao CoreGui
ScreenGui.Parent = game.CoreGui
ScreenGui.ResetOnSpawn = false

-- Configurar Frame
Frame.Parent = ScreenGui
Frame.BackgroundColor3 = Color3.fromRGB(35, 35, 35)
Frame.Size = UDim2.new(0, 400, 0, 400)
Frame.Position = UDim2.new(0.5, -200, 0.5, -200)

-- Configurar NotificacaoLabel
NotificacaoLabel.Parent = Frame
NotificacaoLabel.BackgroundTransparency = 1
NotificacaoLabel.Size = UDim2.new(0, 380, 0, 30)
NotificacaoLabel.Position = UDim2.new(0.5, -190, 0, 10)
NotificacaoLabel.Font = Enum.Font.GothamBold
NotificacaoLabel.Text = "Bem-vindo, " .. game.Players.LocalPlayer.Name .. "!"
NotificacaoLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
NotificacaoLabel.TextSize = 20

-- Configurar TituloLabel
TituloLabel.Parent = Frame
TituloLabel.BackgroundTransparency = 1
TituloLabel.Size = UDim2.new(0, 380, 0, 40)
TituloLabel.Position = UDim2.new(0.5, -190, 0, 50)
TituloLabel.Font = Enum.Font.Gotham
TituloLabel.Text = "Hub Key System"
TituloLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
TituloLabel.TextSize = 24

-- Configurar TextBox
TextBox.Parent = Frame
TextBox.BackgroundColor3 = Color3.fromRGB(45, 45, 45)
TextBox.Size = UDim2.new(0, 380, 0, 50)
TextBox.Position = UDim2.new(0.5, -190, 0, 100)
TextBox.Font = Enum.Font.Gotham
TextBox.PlaceholderText = "Insira a Chave Aqui"
TextBox.TextColor3 = Color3.fromRGB(255, 255, 255)
TextBox.TextSize = 14

-- Configurar BotaoVerificar
BotaoVerificar.Parent = Frame
BotaoVerificar.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
BotaoVerificar.Size = UDim2.new(0, 380, 0, 50)
BotaoVerificar.Position = UDim2.new(0.5, -190, 0, 160)
BotaoVerificar.Font = Enum.Font.Gotham
BotaoVerificar.Text = "Verificar Chave"
BotaoVerificar.TextColor3 = Color3.fromRGB(255, 255, 255)
BotaoVerificar.TextSize = 14

-- Configurar BotaoPegarChave
BotaoPegarChave.Parent = Frame
BotaoPegarChave.BackgroundColor3 = Color3.fromRGB(60, 60, 60)
BotaoPegarChave.Size = UDim2.new(0, 380, 0, 50)
BotaoPegarChave.Position = UDim2.new(0.5, -190, 0, 220)
BotaoPegarChave.Font = Enum.Font.Gotham
BotaoPegarChave.Text = "Pegar Chave"
BotaoPegarChave.TextColor3 = Color3.fromRGB(255, 255, 255)
BotaoPegarChave.TextSize = 14

-- Configurar StatusLabel
StatusLabel.Parent = Frame
StatusLabel.BackgroundTransparency = 1
StatusLabel.Size = UDim2.new(0, 380, 0, 100)
StatusLabel.Position = UDim2.new(0.5, -190, 0.9, -40)
StatusLabel.Font = Enum.Font.Gotham
StatusLabel.Text = ""
StatusLabel.TextColor3 = Color3.fromRGB(255, 255, 255)
StatusLabel.TextSize = 24

-- Restante do código permanece o mesmo
